const should = require('should');

const url = '/add';
const method = 'POST';

describe(`api: ${method} ${url}`, () => {

  it('should return correct sum answer', () => {
    true.should.not.be.false();
  });

});
